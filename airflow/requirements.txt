# airflow/requirements.txt
# Python packages installed in all Airflow containers
# These enable the DAG to connect to Snowflake, AWS, and run dbt/ML

# Snowflake
apache-airflow-providers-snowflake==5.3.0
snowflake-connector-python[pandas]==3.6.0

# AWS
apache-airflow-providers-amazon==8.15.0
boto3==1.34.0

# dbt (runs via BashOperator — needs dbt installed in container)
dbt-snowflake==1.7.0

# ML dependencies
scikit-learn==1.4.0
xgboost==2.0.3
joblib==1.3.2
numpy==1.26.4
pandas==2.2.0
scipy==1.12.0
python-dotenv==1.0.0
pyarrow==14.0.1